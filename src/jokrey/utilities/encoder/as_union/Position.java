package jokrey.utilities.encoder.as_union;

import jokrey.utilities.transparent_storage.TransparentStorage;

public abstract class Position {
    public abstract boolean hasNext(TransparentStorage storage);
}